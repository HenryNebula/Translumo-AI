<UserControl
    x:Class="Translumo.MVVM.Views.LanguagesSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:System="clr-namespace:System;assembly=mscorlib"
    xmlns:controls="clr-namespace:Translumo.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="475" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
            <StackPanel.Resources>
                <Style TargetType="{x:Type StackPanel}">
                    <Setter Property="Margin" Value="0,0,0,20" />
                </Style>
            </StackPanel.Resources>
            <StackPanel Orientation="Horizontal">
                <ComboBox
                    DisplayMemberPath="DisplayName"
                    FontSize="14"
                    ItemsSource="{Binding Path=AvailableLanguages}"
                    SelectedValue="{Binding Path=TranslateFromLang}"
                    SelectedValuePath="LanguageDescriptor.Language"
                    Style="{StaticResource ComboBoxBase}" />
                <Label
                    Margin="10,0,0,0"
                    Content="{DynamicResource Str.LangSettings.SourceLang}"
                    Style="{StaticResource ControlCaptionLabel}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <ComboBox
                    DisplayMemberPath="DisplayName"
                    FontSize="14"
                    ItemsSource="{Binding Path=AvailableTranslationLanguages}"
                    MaxDropDownHeight="300"
                    SelectedValue="{Binding Path=TranslateToLang}"
                    SelectedValuePath="LanguageDescriptor.Language"
                    Style="{StaticResource ComboBoxBase}" />
                <Label
                    Margin="10,0,0,0"
                    HorizontalAlignment="Stretch"
                    Content="{DynamicResource Str.LangSettings.TranslationLang}"
                    Style="{StaticResource ControlCaptionLabel}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <ComboBox
                    Width="130"
                    FontSize="14"
                    ItemsSource="{Binding AvailableTranslators}"
                    SelectedValue="{Binding Path=Model.Translator}"
                    SelectedValuePath="Value"
                    DisplayMemberPath="DisplayName"
                    Style="{DynamicResource MaterialDesignComboBox}" />
                <Label
                    Margin="10,0,0,0"
                    HorizontalAlignment="Stretch"
                    Content="{DynamicResource Str.LangSettings.Translator}"
                    Style="{StaticResource ControlCaptionLabel}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <ComboBox
                    Width="130"
                    FontSize="14"
                    ItemsSource="{Binding AvailableTtsEngines}"
                    SelectedValue="{Binding Path=TtsSystem}"
                    SelectedValuePath="Value"
                    DisplayMemberPath="DisplayName"
                    Style="{DynamicResource MaterialDesignComboBox}" />
                <Label
                    Margin="10,0,0,0"
                    HorizontalAlignment="Stretch"
                    Content="{DynamicResource Str.LangSettings.TtsSystem}"
                    Style="{StaticResource ControlCaptionLabel}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Visibility="{Binding IsTtsWindowsSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ComboBox 
                    ItemsSource="{Binding AvailableVoices}"
                    SelectedItem="{Binding SelectedVoice, Mode=TwoWay}"
                    DisplayMemberPath="Name"
                    Width="130"
                    FontSize="14"
                    Style="{DynamicResource MaterialDesignComboBox}"/>
                <Label
                    Margin="10,0,0,0"
                    HorizontalAlignment="Stretch"
                    Content="{DynamicResource Str.LangSettings.Voice}"
                    Style="{StaticResource ControlCaptionLabel}" />
            </StackPanel>

            <GroupBox Margin="0,20,0,0" Style="{StaticResource MaterialDesignGroupBox}">
                <GroupBox.Header>
                    <Label
                        Margin="0,-7,0,-7"
                        Content="{DynamicResource Str.ProxySettings.Proxy}"
                        Style="{StaticResource SecondaryHeaderLabel}" />
                </GroupBox.Header>
                <StackPanel>
                    <TextBlock
                        FontSize="12"
                        Style="{StaticResource TextBlockBase}"
                        Text="{DynamicResource Str.ProxySettings.Note}"
                        TextWrapping="WrapWithOverflow" />
                    <Button
                        Margin="0,5,0,0"
                        HorizontalAlignment="Left"
                        Command="{Binding ProxySettingsClickedCommand}"
                        Content="{DynamicResource Str.Button.SettingProxy}"
                        Style="{StaticResource PrimaryButton}" />
                </StackPanel>
            </GroupBox>

            <GroupBox
                Margin="0,20,0,0"
                Style="{StaticResource MaterialDesignGroupBox}"
                Visibility="{Binding IsLlmSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                <GroupBox.Header>
                    <Label
                        Margin="0,-7,0,-7"
                        Content="{DynamicResource Str.LlmSettings.Header}"
                        Style="{StaticResource SecondaryHeaderLabel}" />
                </GroupBox.Header>
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,8"
                        FontSize="12"
                        Style="{StaticResource TextBlockBase}"
                        Text="{DynamicResource Str.LlmSettings.Note}"
                        TextWrapping="WrapWithOverflow" />
                    <Button
                        Margin="0,5,0,0"
                        HorizontalAlignment="Left"
                        Command="{Binding ManageApiClickedCommand}"
                        Content="{DynamicResource Str.Button.ManageApi}"
                        Style="{StaticResource PrimaryButton}" />
                </StackPanel>
            </GroupBox>
            </StackPanel>
            <DockPanel
                Grid.Row="0"
                Grid.Column="1"
                Margin="9,0,0,0"
                Visibility="{Binding ProxySettingsIsOpened, Converter={StaticResource BooleanToVisibilityConverter}}">
                <controls:ProxyEditList
                    x:Name="ProxyListControl"
                    Padding="9,0,0,0"
                    HorizontalAlignment="Stretch"
                    AddCommand="{Binding ProxyItemAddCommand}"
                    BorderBrush="{StaticResource PrimaryHueMidBrush}"
                    BorderThickness="1,0,0,0"
                    DeleteCommand="{Binding ProxyItemDeletedCommand}"
                    SourceList="{Binding Path=ProxyCollection, Mode=TwoWay}"
                    SubmitCommand="{Binding ProxySettingsSubmitCommand}"
                    UseLayoutRounding="False" />
            </DockPanel>

            <DockPanel
                Grid.Row="0"
                Grid.Column="1"
                Margin="9,0,0,0"
                Visibility="{Binding LlmSettingsIsOpened, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel
                        Grid.Row="0"
                        Margin="0,-6,0,20"
                        Orientation="Horizontal">
                        <Button
                            Command="{Binding CloseLlmPanelCommand}"
                            Content="{materialDesign:PackIcon KeyboardBackspace}"
                            Style="{StaticResource FlatLeftButton}" />
                        <Label
                            Content="{DynamicResource Str.LlmSettings.PanelCaption}"
                            Style="{StaticResource PanelCaptionLabel}" />
                    </StackPanel>
                    <ScrollViewer
                        Grid.Row="1"
                        Margin="0,0,4,0"
                        Style="{DynamicResource MaterialDesignScrollViewer}"
                        HorizontalScrollBarVisibility="Disabled"
                        VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <StackPanel
                                Margin="0,0,0,10"
                                Orientation="Horizontal">
                                <Button
                                    Margin="0,0,8,0"
                                    Command="{Binding AddLlmProfileCommand}"
                                    Content="{DynamicResource Str.LlmSettings.AddProfile}"
                                    Style="{StaticResource PrimaryButton}" />
                                <Button
                                    Command="{Binding DeleteLlmProfileCommand}"
                                    Content="{DynamicResource Str.LlmSettings.DeleteProfile}"
                                    Style="{StaticResource PrimaryButton}" />
                            </StackPanel>
                            <ComboBox
                                Width="200"
                                HorizontalAlignment="Left"
                                FontSize="13"
                                Margin="0,0,0,8"
                                ItemsSource="{Binding LlmProfileNames}"
                                SelectedItem="{Binding SelectedLlmProfileName}"
                                Style="{DynamicResource MaterialDesignComboBox}" />
                            <StackPanel Margin="0,0,0,8">
                                <Label
                                    Margin="0,0,0,2"
                                    Content="{DynamicResource Str.LlmSettings.Provider}"
                                    Style="{StaticResource ControlCaptionLabel}" />
                                <ComboBox
                                    Width="200"
                                    HorizontalAlignment="Left"
                                    FontSize="14"
                                    ItemsSource="{Binding AvailableLlmProviders}"
                                    SelectedValue="{Binding Path=LlmSettings.Provider}"
                                    SelectedValuePath="Value"
                                    DisplayMemberPath="DisplayName"
                                    Style="{DynamicResource MaterialDesignComboBox}" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,8">
                                <Label
                                    Margin="0,0,0,2"
                                    Content="{DynamicResource Str.LlmSettings.ApiKey}"
                                    Style="{StaticResource ControlCaptionLabel}" />
                                <TextBox
                                    Width="430"
                                    HorizontalAlignment="Left"
                                    FontSize="13"
                                    Style="{StaticResource TextBoxBase}"
                                    Text="{Binding Path=LlmSettings.ApiKey, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,8">
                                <Label
                                    Margin="0,0,0,2"
                                    Content="{DynamicResource Str.LlmSettings.Endpoint}"
                                    Style="{StaticResource ControlCaptionLabel}" />
                                <TextBox
                                    Width="430"
                                    HorizontalAlignment="Left"
                                    FontSize="13"
                                    Style="{StaticResource TextBoxBase}"
                                    Text="{Binding Path=LlmSettings.Endpoint, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,8">
                                <Label
                                    Margin="0,0,0,2"
                                    Content="{DynamicResource Str.LlmSettings.Model}"
                                    Style="{StaticResource ControlCaptionLabel}" />
                                <ComboBox
                                    Width="430"
                                    HorizontalAlignment="Left"
                                    FontSize="13"
                                    IsEditable="True"
                                    StaysOpenOnEdit="True"
                                    ItemsSource="{Binding AvailableModels}"
                                    Text="{Binding Path=LlmSettings.ModelName, UpdateSourceTrigger=PropertyChanged}"
                                    Style="{DynamicResource MaterialDesignComboBox}" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,8">
                                <CheckBox
                                    Margin="2,0,0,0"
                                    VerticalAlignment="Center"
                                    IsChecked="{Binding Path=LlmSettings.UseVisionForImages}">
                                    <CheckBox.Content>
                                        <TextBlock
                                            FontSize="13"
                                            Text="{DynamicResource Str.LlmSettings.UseVisionForImages}"
                                            TextWrapping="Wrap" />
                                    </CheckBox.Content>
                                </CheckBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button
                                    Margin="0,4,0,0"
                                    HorizontalAlignment="Left"
                                    Command="{Binding TestLlmConnectionCommand}"
                                    Content="{DynamicResource Str.LlmSettings.Test}"
                                    Style="{StaticResource PrimaryButton}" />
                                <TextBlock
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="Green"
                                    Style="{StaticResource TextBlockBase}"
                                    Text="{Binding LlmTestStatus}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                    <Button
                        Grid.Row="2"
                        Content="{DynamicResource Str.Button.Done}"
                        Style="{StaticResource OutlinedBottomButton}"
                        Command="{Binding CloseLlmPanelCommand}" />
                </Grid>
            </DockPanel>
        </Grid>
</UserControl>
